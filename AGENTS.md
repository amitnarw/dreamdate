# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# PROJECT CONTEXT

**Read `PROJECT.md` first** , it captures the full app architecture, design system ("Borderless Tonal" language , no borders, minimal shadows only on navbar/modals/back button), engagement/psychology funnel (notifications, in-app incoming call, low-balance nudges, chat realism engine), storage keys registry, established user preferences (no archetype UI labels, no shadow-everywhere, no border-everywhere), and what NOT to do.
